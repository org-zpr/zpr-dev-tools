name: PR Notifications

on:
  pull_request:
    types:
      - review_requested

jobs:
  notify:
    uses: org-zpr/zpr-dev-tools/.github/workflows/pr-review-notify.yml@add-slack-pr-test
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_ALERT_WEBHOOK_URL }}

